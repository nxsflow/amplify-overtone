---
"@nxsflow/amplify-overtone": minor
---

Add `n.email()` schema builder for declarative email actions with AppSync mutation generation, JS resolvers, sender validation, and subject template compilation
