---
"@nxsflow/amplify-overtone": minor
---

Add email action schema types for `n.email()` builder

Introduces `EmailActionConfig`, `EmailActionBuilder`, `CompiledEmailAction`, `AuthRule`, `FieldDef`, and supporting types needed for declarative email actions in the schema builder.
