version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22
        - npm ci
    build:
      commands:
        - npm run website:build
  artifacts:
    baseDirectory: packages/website/out
    files:
      - "**/*"
  cache:
    paths:
      - node_modules
